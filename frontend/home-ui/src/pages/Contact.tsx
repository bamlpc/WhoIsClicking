//import { useTranslation } from 'react-i18next';

const Contact = () => {
  //const { t, i18n } = useTranslation();
  //<h1>{t('welcome_message')}</h1>

  return (
    <div className="App-header">
      <h1> Our contact information in linked in and github </h1>
    </div>
  );
};

export default Contact;
