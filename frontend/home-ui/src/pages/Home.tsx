//import { useTranslation } from 'react-i18next';

const Home = () => {
  //const { t, i18n } = useTranslation();
  //{t('welcome_message')}
  return (
    <div className="App-header">
      <h1>welcome page</h1>
    </div>
  );
};

export default Home;
