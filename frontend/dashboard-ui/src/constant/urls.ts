const BASE_URL = 'http://localhost:8000/api/';

const LOGIN_URL = `${BASE_URL}login`;
const LOGOUT_URL = `${BASE_URL}logout`;
const SIGNIN_URL = `${BASE_URL}register`;

export { LOGIN_URL, LOGOUT_URL, SIGNIN_URL };
